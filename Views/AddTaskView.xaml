<UserControl x:Class="ToDoList.Views.AddTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
    <Grid Background="#CAEDFF">
        <Grid.RowDefinitions>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Added Task -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#A3BDDB" CornerRadius="25" Width="400" Height="80" Margin="0,25,0,0">
            <TextBlock Text="Add Task" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Margin="0,0,0,0" Width="120"  Height="40" FontWeight="Bold" FontFamily="Verdana"/>
        </Border>

        <Border Grid.Row="1" Background="#6C55C1" CornerRadius="25" Width="400" Height="80" Margin="0,25,0,0">
            <Border Background="#A3BDDB" CornerRadius="25" Padding="0">
                <TextBox HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Margin="0,0,0,0" Width="380"  Height="50" FontFamily="Calibri" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Border>
        </Border>

        <Button Grid.Row="2" Content="Add" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" 
                Margin="0,25,0,0" Width="120"  Height="55" FontWeight="Bold" FontFamily="Verdana" 
                Background="Black" Foreground="White" BorderBrush="#6C55C1">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="1" 
                                        CornerRadius="25">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

    </Grid>
</UserControl>
